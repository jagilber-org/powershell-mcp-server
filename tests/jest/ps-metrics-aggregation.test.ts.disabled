/**
 * TEMP DISABLED: See ps-metrics-aggregation.test.js header for rationale.
 * Retained for future re-enable once deterministic sampling path added.
 */
describe('integration ps metrics aggregation (disabled)', ()=>{ test.skip('pending stabilization', ()=>{}); });
export {}; // ensure module context

/* Legacy implementation removed for now */